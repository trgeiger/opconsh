# Operator Controller Shell

Experimental read-eval-print loop shell for the [operator-controller](https://github.com/operator-framework/operator-controller).

## Building
```sh
go build -o opconsh .
```

## Using
`opconsh` reads from your KUBECONFIG 